# Magic ball

The user asks a yes–no question to the ball, and the program answers it.

### Requirements

`ruby >= 2.7.0`

### Installation & using

In `terminal` or `cmd`

```
ruby magic_ball.rb
```
